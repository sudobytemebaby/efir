api:
  dashboard: true
  insecure: true

ping: {}

entryPoints:
  web:
    address: ":80"
  websecure:
    address: ":443"

providers:
  docker:
    network: efir-network
    exposedByDefault: false
  file:
    directory: /etc/traefik/dynamic
    watch: true
