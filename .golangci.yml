linters-settings:
  errcheck:
    check-type-assertions: true
  govet:
    check-shadowing: true
  staticcheck:
    checks: ["all"]
  unused:
    checks: ["all"]

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - gofmt
    - unused

run:
  timeout: 5m
  tests: true
