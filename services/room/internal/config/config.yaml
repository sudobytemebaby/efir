env: development
log_level: info

server:
  grpc_port: "50053"
  health_listen_addr: ":8080"

timeouts:
  shutdown: 30s
  read_header: 5s
  grpc_graceful: 5s

nats:
  url: "nats://localhost:4222"
  user: ""
  reconnect_wait: 2s
  max_reconnects: -1
